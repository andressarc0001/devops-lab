import { decodeJwt } from "./decodeJwt.mjs";
import { assert } from "../tools/tsafe/assert.mjs";
import { INFINITY_TIME } from "./INFINITY_TIME.mjs";
// Return undefined if token provided wasn't a JWT or if it hasn't an exp claim number
export function readExpirationTimeInJwt(token) {
    let exp;
    try {
        exp = decodeJwt(token).exp;
        assert(typeof exp === "number");
    }
    catch {
        return undefined;
    }
    if (exp === 0) {
        return INFINITY_TIME;
    }
    return exp * 1000;
}
//# sourceMappingURL=readExpirationTimeInJwt.mjs.map
