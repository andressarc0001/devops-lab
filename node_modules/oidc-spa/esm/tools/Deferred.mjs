export class Deferred {
    constructor() {
        let resolve;
        let reject;
        let valueWrap = undefined;
        this.pr = new Promise((resolve_, reject_) => {
            resolve = value => {
                if (valueWrap !== undefined) {
                    return;
                }
                valueWrap = [value];
                resolve_(value);
            };
            reject = error => {
                reject_(error);
            };
        });
        this.resolve = resolve;
        this.reject = reject;
        this.getState = () => valueWrap === undefined
            ? { hasResolved: false }
            : { hasResolved: true, value: valueWrap[0] };
    }
}
export class VoidDeferred extends Deferred {
}
//# sourceMappingURL=Deferred.mjs.map